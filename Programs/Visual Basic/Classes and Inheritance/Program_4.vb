Imports System

Module Program
    Class Worker
        Public Name As String
        Public Gender As Boolean
        Public Salary As Integer
        Public Origin As String
    End Class
    Sub Main(args As String())
        Dim nm As New Worker
        Dim gender As New Worker
        Dim sal As New Worker
        Dim org As New Worker

        Console.ReadKey()
    End Sub
End Module
